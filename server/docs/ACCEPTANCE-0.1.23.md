# 0.1.23 生产 Python 服务端验证记录

验证日期：2026-09-22。

- 完整 Python 服务端、真实 MySQL 8.4.11 集成及 0.1.23 升级脚本测试：129 通过，6 跳过。
- 跳过项：1 项属于独立旧部署协调器，5 项需要本机未安装的 .NET 互操作运行时；Python/MySQL 协议、分析接口和升级流程均已覆盖。
- 控制台验证覆盖：只显示当前目标服务、白名单目标精确识别、当前异常恢复、上次运行结果标记、聚合与下钻一致、`providerMode` 被拒绝。
- 升级验证覆盖：从 0.1.22/0.1.21 识别来源、离线架构依赖、配置继承、可选数据库备份、准备失败不停止旧服务、切换失败恢复、systemd 托管保护和回退来源持久化。
- 发布包验证包括逐文件 manifest、ZIP 完整性、SHA256 清单以及敏感文件排除。

本次没有数据库结构迁移，也没有修改客户端协议。Linux 真实服务器切换仍需按升级说明完成现场状态、健康接口和控制台验收。
