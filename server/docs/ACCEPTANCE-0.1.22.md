# 0.1.22 生产 Python 服务端验证记录

验证日期：2026-09-18。环境：macOS arm64、Python 3.13.15、隔离 MySQL 8.4.10（127.0.0.1:13306）、.NET SDK 10.0.400。未连接或修改生产服务器/数据库。

- Python完整套件：113通过、1跳过。唯一跳过项是独立旧部署工具deploy.py的额外数据库快照测试，该工具不包含于本次交付；新版备份有实际MySQL验证。
- 0.1.22升级脚本：11通过，模拟验证来源选择、systemd托管拒绝、配置/凭证继承、两架构离线安装、准备失败、切换失败恢复、回退来源、路径/manifest和备份保护。
- 实际MySQL备份：旧配置加载、数据库导出、保护目录权限、凭证不输出、策略不变和原backup-state不修改。
- 原加密协议回归：Python/.NET双向AES-GCM、原始签名、真实Gunicorn HTTP客户端注册、心跳、事件、命令及重启防重放通过。命令回执为模拟执行，不代表真实终端启停。
- 30个测试终端的Python/MySQL浏览器联动：登录、默认数据分析、25+5分页、下钻转资产、筛选、详情返回、实际远程命令创建与确认、409保留草稿、HTTP网关保存、390px布局、无JS错误。
- Linux x86_64与aarch64、CPython3.13离线依赖解析均通过，不需要访问包仓库。
- 不改变schema、客户端协议、生产凭证或已有公开接口；不采集新请求或历史数据。

真实Linux服务器上的进程切换、企业网络、自启动安排和生产数据表现尚未运行，需要按交付说明现场验收。原始测试结果及测试截图位于server/artifacts，未包含在升级/源码ZIP。历史ACCEPTANCE.md保留旧版本证据，本版以本文件为准。
